// this a test code and this file hasn't been used yet in the current release

function testData(data){
    var final= 'it got to finder.js ' + data;
    return final;
}

module.exports = {
    testData
}
